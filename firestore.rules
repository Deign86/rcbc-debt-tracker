rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all operations for single-user mode (no auth required)
    // The app uses fixed document ID 'current' for all collections
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
